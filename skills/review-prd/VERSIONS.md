# Review-PRD Skill — Version History

## v2.11
Feedback from five CONVERGE + COMPREHENSIVE runs (action-portal v5.0, action-core v6.0, action-views v1.0, action-dashboard STANDARD, evidence-source-links v4.2). (1) Edit not Write — full file rewrites prohibited; use targeted Edit calls grouped by fix category. (2) Content additions = DECISION — adding new goals/NFRs/FRs to hit count minimums is authoring, not mechanical; must preview before writing. (3) Phase 4 minimum spot-check — adversarial-test at least 3 Must Have FRs and 2 Tier 1 UCs with explicit pass/fail reasoning. (4) Scope-depth mismatch pushback — COMPREHENSIVE depth on STANDARD-scope PRD should recommend matching depth to scope. (5) Richer Round 0 summary — per-finding table with one-line descriptions before fixing. (6) Review scope boundary — don't create new artifacts (UCs, FRs); flag as FAIL and defer to /prd. (7) Reference persona support aligned with /prd v3.10.

## v2.10
Feedback from two CONVERGE + COMPREHENSIVE runs (identity-integration, evidence-source-links). (1) Post-fix corruption scan — grep for concatenated lines, orphaned fences, duplicate headings after each fix round. (2) Internal consistency check — verify budget statements, traceability tables, and count references after FR/NFR additions. (3) Justified deviation inline waivers — documented justification paragraphs (e.g., "13 Must Haves rationale") function as permanent acceptances, not re-flagged on subsequent runs. (4) Reclassification transparency — FAIL→WARN downgrades must state reason in summary with `↓` marker. (5) Fix-immediately guidance — start obvious mechanicals before full analysis completes.

## v2.9
Feedback from CONVERGE + COMPREHENSIVE on escalation-rule-configuration PRD. (1) CONVERGE fix verification — grep immediately after replace_all to catch line-merge bugs; grep for stale references after cascading renames (persona names, FR IDs). (2) Phase 4 as separate read pass — adversarial depth must not be merged into a single pass with Phases 1-3; re-read FRs/UCs/assumptions with fresh adversarial focus after structural checks complete.

## v2.8
Feedback from CONVERGE + COMPREHENSIVE on a v2.1 PRD. (1) WARN triage "fix all" shortcut — before per-batch review, offer Fix all / Review individually / Accept all gate; reduces overhead when all WARNs are minor. (2) Phase 1 chunking optional in CONVERGE — sequential reading acceptable for speed; chunking mandatory only for interactive COMPREHENSIVE or PRDs >1000 lines. (3) TOC anchor verification — after renaming sections or restructuring headings, verify TOC `(#anchor)` links still resolve.

## v2.7
Feedback from CONVERGE + COMPREHENSIVE on a mature v4.0 PRD. (1) Time allocation inversion — mature PRDs get 20% Phase 1 structural, 40% Phase 4 adversarial; if Phase 4 produces 0 findings on a mature PRD, the review likely wasn't thorough enough. (2) Phase 3 strengthened — read actual ADR files the PRD cites (not just index), verify claims match ADR content, check ADR status; sibling module spot-check for COMPREHENSIVE (read 50-100 lines of one sibling PRD for convention consistency). (3) Domain-specific adversarial probes added to Phase 4 — race conditions/concurrency, arbitrary limits justification, unbounded growth, seed data sufficiency, cross-module boundary ambiguity, cascading failure modes.

## v2.6
Feedback from three more CONVERGE + COMPREHENSIVE runs (workflow-configuration v3.0, tenant-configuration v3.3, role-management). (1) Maturity discount — cosmetic template formatting violations (numbering, heading level, section nesting) downgraded from FAIL to WARN for mature PRDs (v2.0+ with prior review evidence); content/security violations remain FAIL. (2) Finding deduplication — same-type violations reported as 1 finding with N locations, not N findings; enables batch fixing. (3) Persona alignment is always DECISION — mapping preview via AskUserQuestion before cascading renumber; "keep local numbering with mapping note" option avoids disproportionate edits. (4) WARN reconciliation — auto-resolve WARNs already fixed as side effects of FAIL fixes before triage. (5) Edit batching guidance — group same-type fixes into 3-5 editing passes, not one Edit per finding. (6) Direct reads for context loading — no Explore agents for file reading. (7) Concise Document History — one-line changelog for CONVERGE version bumps.

## v2.5
Feedback from two CONVERGE + COMPREHENSIVE runs (auth PRD v0.1, language management PRD v6.3). (1) Missing cross-cutting PRD flagged as WARN — explains Phase 3 coverage limitations upfront instead of silently skipping checks. (2) Maturity-aware depth recommendation — mature PRDs (5+ versions) get STANDARD suggestion even when COMPREHENSIVE requested; gate explains diminishing returns. (3) CONVERGE depth auto-downgrade — if Round 0 finds only structural FAILs, skips Phase 4 re-run on subsequent rounds. (4) FR count minimum downgraded from Warning to INFO — narrow well-decomposed modules shouldn't be penalized for having fewer FRs than the threshold. (5) WARN triage transparency — excluded WARNs listed with rationale before triage question; multi-batch enforced for >4 actionable WARNs. (6) All Tier 1 UCs must be read for COMPREHENSIVE — no sampling, mandatory full read.

## v2.4
Feedback from COMPREHENSIVE + CONVERGE review of BFF NuGet PRD. (1) PRD origin detection — distinguishes skill-generated PRDs from hand-written; shifts review weight toward adversarial depth for well-structured PRDs. (2) Must Have ≤10 downgraded from Warning to INFO — heuristic doesn't apply well to security-critical packages; post-fix additions don't trigger. (3) Quality-adaptive fast path — if Phase 1 ≥95% pass, skip naming/heading checks in Phase 2 and focus on adversarial depth. (4) UC cross-reference integrity — deeper verification of UC-to-FR traceability, FR-to-UC back-references, UC content alignment with OQ resolutions, and UC-to-UC consistency. (5) Compact CONVERGE report as default — full table only for >3 rounds or >10 findings. (6) Package API Contract variant recognized in Phase 1.8 Integration Points checks. (7) Library & Package PRD note alongside Policy PRD note. (8) /prd version references updated to v3.8.

## v2.3
Production feedback from API Keys CONVERGE + COMPREHENSIVE run. (1) READ-ONLY vs CONVERGE contradiction fixed — READ-ONLY scoped to non-CONVERGE modes. (2) Phase 1 chunking strategy for PRDs >300 lines (3 passes: metadata+problem, FRs+NFRs, prioritisation+validation). (3) WARN triage step after 0 FAILs — present remaining WARNs for user resolution. (4) NFR-AUDIT template content for mechanical fixes. (5) Phase 4 severity guide table (FAIL vs WARN per finding type). (6) Rubber Stamp anti-pattern updated — low Phase 1 findings on revised PRDs (v1.2+) is a quality signal, not insufficient depth. (7) Convergence report template for consistent round-by-round reporting.

## v2.2
CONVERGE mode refined — skip interactive stage gates, replace per-finding walkthrough with summary table, WARNs listed but not interactive. Chunked reading guidance for large PRDs.

## v2.1
CONVERGE mode added — autoresearch loop built into the review skill. Runs review at selected depth, classifies findings (MECHANICAL/JUSTIFIED_DEVIATION/DECISION), auto-fixes mechanical issues, re-reviews until 0 FAILs or convergence. Authority hierarchy specific to PRDs.

## v2.0
Phase 1 checklist fully synced with /prd v3.7 Structural Conventions — now checks exact heading formats, numbering prefixes (G/NG/A/C), heading levels (H2/H3/H4), all 6 persona sub-fields individually, FR/NFR body structure lines, mandatory audit NFR, strict NFR minimum (Fail not Warning), MoSCoW exact headings, Integration Points sub-headings, Document Approval table columns, Dependency Graph with ASCII arrows. Phase 2 adds naming convention consistency, heading level compliance, and audit coverage checks. Policy & Standards PRD exceptions noted. Template Worship anti-pattern reconciled with non-negotiable structural conventions. TOC upgraded from Warning to Fail for COMPREHENSIVE.
