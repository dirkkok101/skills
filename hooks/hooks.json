{
  "hooks": [
    {
      "event": "session_start",
      "command": "bash hooks/session-start",
      "description": "Inject workflow skill awareness at session start",
      "cwd": "{{plugin_dir}}"
    }
  ]
}
